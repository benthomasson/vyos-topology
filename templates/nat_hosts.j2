[switch]
{%for device in devices%}
{%if device.type == "switch" -%}
{{device.name}} ansible_host={{device.nat_ip}} ansible_port={{device.nat_port}} ansible_user=vagrant
{%-endif%}
{%endfor%}
[host]
{%for device in devices%}
{%if device.type == "host"-%}
{{device.name}} ansible_host={{device.nat_ip}} ansible_port={{device.nat_port}} ansible_user=vagrant
{%-endif%}
{%endfor%}
