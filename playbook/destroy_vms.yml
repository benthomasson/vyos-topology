---
- name: Destroy VMs on hypervisor
  hosts: hypervisor
  gather_facts: false
  tasks:
      - ping:
      - name: Destroy VMs
        shell: "{{workspace}}/bin/ansible-playbook playbook/down.yml"
        args:
          chdir: "{{workspace}}"
