---
- name: Destroy VMs on hypervisor
  hosts: hypervisor
  gather_facts: false
  tasks:
      - ping:
      - name: Destroy VMs
        shell: "{{virtualenv}}/bin/ansible-playbook playbook/down.yml"
        args:
          chdir: "{{workspace}}"
